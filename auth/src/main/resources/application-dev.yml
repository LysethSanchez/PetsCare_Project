server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petscare_auth?useSSL=false&serverTimezone=UTC
    username: root
    password: 1se2bas3tian4#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

app:
  cors:
    allowed-origins: "*"
